const ws = require('ws');
const express = require('express');

if (require.main === module) {
    return server();
}

/**
 * @param {Object} args
 * @param {Config} config
 * @param {function} cb
 */
async function server(args, config, cb) {

}
