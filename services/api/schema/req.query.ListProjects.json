{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "default": 100,
            "description": "Limit number of returned runs"
        },
        "page": {
            "type": "integer",
            "default": 0,
            "description": "The offset based on limit to return"
        },
        "sort": {
            "type": "string",
            "default": "desc",
            "description": "Sorting order for listing projects based on created timestamp. Allowed 'desc' and 'asc'. Default desc."
        },
        "name": {
            "type": "string",
            "default": "",
            "description": "Filter projects that matches the name."
        }
    }
}
