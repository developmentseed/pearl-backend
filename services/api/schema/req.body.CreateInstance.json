{
    "type": "object",
    "required": [ ],
    "additionalProperties": false,
    "properties": {
        "aoi_id": {
            "type": "integer",
            "description": "The current AOI loaded on the instance"
        },
        "checkpoint_id": {
            "type": "integer",
            "description": "The current checkpoint loaded on the instance"
        },
        "type": {
            "type": "string",
            "description": "The type of the instance. cpu or gpu",
            "default": "cpu"
        }
    }
}
