{
    "type": "object",
    "required": [ ],
    "additionalProperties": false,
    "properties": {
        "timeframe_id": {
            "type": "integer",
            "description": "The current TimeFrame loaded on the instance"
        },
        "checkpoint_id": {
            "type": "integer",
            "description": "The current checkpoint loaded on the instance"
        },
        "type": {
            "type": "string",
            "description": "The type of the instance. cpu or gpu",
            "default": "cpu"
        }
    }
}
