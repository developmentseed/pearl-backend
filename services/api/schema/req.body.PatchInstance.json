{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "active": {
            "type": "boolean",
            "description": "Is the instance currently running"
        },
        "aoi_id": {
            "type": "integer",
            "description": "The current AOI loaded on the instance"
        },
        "checkpoint_id": {
            "type": "integer",
            "description": "The current checkpoint loaded on the instance"
        }
    }
}
