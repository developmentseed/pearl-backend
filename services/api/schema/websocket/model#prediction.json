{
    "type": "object",
    "additionalProperties": false,
    "required": [
        "action",
        "data"
    ],
    "properties": {
        "action": {
            "type": "string",
            "enum": ["model#prediction"]
        },
        "data": {
            "type": "object",
            "additionalProperties": false,
            "required": [ "aoi_id", "mosaic" ],
            "properties": {
                "aoi_id": {
                    "type": "integer",
                    "description": "AOI to process with"
                },
                "mosaic": {
                    "type": "string",
                    "description": "Mosaic to process the AOI with"
                }
            }
        }
    }
}

