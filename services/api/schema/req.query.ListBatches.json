{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "$ref": "./util/limit.json"
        },
        "page": {
            "$ref": "./util/page.json"
        },
        "order": {
            "$ref": "./util/order.json"
        },
        "completed": {
            "type": "boolean",
            "description": "Filter by completed status"
        },
        "sort": {
            "type": "string",
            "default": "created",
            "enum": [
                "id",
                "created",
                "updated",
                "aoi",
                "name",
                "completed"
            ],
            "description": "Field to sort order by"
        }
    }
}
