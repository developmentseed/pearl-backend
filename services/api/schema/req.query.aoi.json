{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "default": 100,
            "description": "Limit number of returned aois"
        },
        "page": {
            "type": "integer",
            "default": 0,
            "description": "The offset based on limit to return"
        },
        "checkpointid": {
            "type": "integer",
            "default": 0,
            "description": "Only return AOIs for a specific checkpoint"
        },
        "bookmarked": {
            "type": "boolean",
            "description": "Only return AOIs that have been bookmarked"
        }
    }
}
