{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "default": 100,
            "description": "Limit number of returned aois"
        },
        "page": {
            "type": "integer",
            "default": 0,
            "description": "The offset based on limit to return"
        },
        "checkpointid": {
            "type": "integer",
            "default": 0,
            "description": "Only return AOIs for a specific checkpoint"
        },
        "bookmarked": {
            "type": "string",
            "default": "",
            "description": "Filter AOIs based on bookmarked. Allowed 'true' or 'false'. By default returns all."
        },
        "sort": {
            "type": "string",
            "default": "desc",
            "description": "Sorting order for listing AOIs based on created timestamp. Allowed 'desc' and 'asc'"
        }
    }
}
