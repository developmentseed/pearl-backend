{
    "type": "object",
    "required": [
        "name",
        "model_id"
    ],
    "additionalProperties": false,
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the Project"
        },
        "model_id": {
            "type": "integer",
            "description": "Starter Model to use for project"
        }
    }
}
